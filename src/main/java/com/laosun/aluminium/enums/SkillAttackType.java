package com.laosun.aluminium.enums;

/**
 * The target selection scope of a skill attack.
 */
public enum SkillAttackType {
    /** Targets a single enemy. */
    SINGLE,
    /** Targets three enemies (blast). */
    THREE,
    /** Targets all enemies on the field. */
    ALL
}
